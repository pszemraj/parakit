# Python dependencies for the maintainer source-rebuild path:
# `parakit fetch --from-source`.
#
# The Rust daemon and default hosted-model fetch do not use these packages.
# They are only needed to run vendor/CrispASR/models/convert-parakeet-to-gguf.py.
gguf
numpy
sentencepiece
torch
